<%= link_to '新規登録', new_user_path %>

<% @users.each do |user| %>
<p>
    <%= user.uid%>
    <%= user.pass%>
    <%= link_to '削除', user_path(user) ,method: :delete %>
</p>
<% end %>

<br><%= link_to 'ツイート一覧に戻る', root_path %>